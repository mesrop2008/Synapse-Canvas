# --- Runtime dependencies ---------------------------------------------------
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32

# SQLAlchemy 2.0 async ORM + PostgreSQL driver
sqlalchemy[asyncio]>=2.0.36,<2.1
asyncpg>=0.30
alembic>=1.14

# Settings / validation
pydantic>=2.9,<3.0
pydantic-settings>=2.6
email-validator>=2.2

# Auth
passlib>=1.7.4
# Pinned <5: passlib 1.7.4 reads bcrypt.__about__, which bcrypt 5.x removed
# outright. 4.x only warns, 5.x breaks the backend probe. See README.
bcrypt>=4.1,<5
pyjwt>=2.9
